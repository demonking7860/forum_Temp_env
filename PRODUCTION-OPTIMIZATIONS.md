# Production Optimizations for Unipanda

## Build Process

1. Run the production build with `npm run build` or `yarn build`
2. Enable gzip/Brotli compression on the server
3. Configure proper cache headers for static assets

## Environment Variables

Create a `.env.production` file with the following:
